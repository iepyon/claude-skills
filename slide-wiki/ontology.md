<!-- 生成物: src/tools/gen-ontology-doc.ts による ontology.yaml からの機械生成。手編集禁止。
     `npx tsx src/tools/gen-ontology-doc.ts` で再生成する。正本は ontology.yaml。
     ontology-version: 9 -->

# slide-wiki スライド Markdown オントロジー

slide-wiki が読む Markdown の構造の全文リファレンス。**この文書は生成物**で、正本は
[ontology.yaml](ontology.yaml)。書き方の要約は [SKILL.md](SKILL.md) にある。

## md の骨格要素

| 要素 | 記号 | 説明 |
|---|---|---|
| `deck`（デッキ） | — | 1つの Markdown ファイル。`---` で区切られたスライドの列。 |
| `slide-break`（スライド区切り） | `---` | スライドの境目。行全体が `---` のときだけ区切りになる。 |
| `title-slide`（タイトルスライド） | `#` | H1 で始まるスライド。直後の本文行がサブタイトルになる。 |
| `content-slide`（コンテンツスライド） | `##` | H2 で始まるスライド。レイアウトは directive で選ぶ（無指定なら Default）。 |
| `section`（セクション） | `###` | スライド内の区画。何を意味するかは選んだレイアウトの slots が決める。 |
| `subsection`（サブセクション） | `####` | 区画の中の行ラベル。CustomerJourney だけが使う。 |
| `image`（画像） | `![alt](src)` | 行まるごとの画像参照。中身は生成時に読み込んで埋め込む。 |
| `body`（本文） | — | 見出しに属する本文行。`- ` `* ` `+ ` の箇条書きと `1. ` の番号付きリストを含む。 |

- `deck` — デッキは冒頭に YAML frontmatter を持てる（→ frontmatter 節）。認識には条件があり、 **1行目がちょうど `---` で、2行目が `key:` の形**のときだけメタとして読む。 どちらかを満たさない `---` は今までどおりスライド区切りなので、 2行目が見出しの md（`---` の直後に `# タイトル`）は1枚目を失わない。

- `title-slide` — デッキ名（Wiki のサイドバー見出し）は先頭のタイトルスライドから採られる。

- `image` — パスは**その md ファイルからの相対**。図解の枠を持つレイアウト（WikiPattern）だけが読み、 他のレイアウトでは描かれない（lint が報告する）。外部ファイルにしてあるのは、 **md をそのまま GitHub で開いても図が絵として表示される**ようにするため — 図の中身を md に書き込むと、読み手には SVG のソースが見えるだけになる。

- `body` — PPTX はネイティブのバレット/自動番号、HTML は CSS 疑似要素で記号を描く。 **リテラルの `•` を書いてはいけない**（記号が二重に表示される）。ネストは1階層のみ。

## デッキのメタ（frontmatter）

md の1行目の `---` から次の `---` まで。中身は YAML のマップ。

| キー | level | 形 | 効き先 | 説明 |
|---|---|---|---|---|
| `type` | required | `text` | lint と外部ツール | このファイルが何であるか。**OKF が唯一必須とするキー**（SPEC.md §4.1）で、 読む側は種別で振り分ける。値は中央登録制ではないので、未知の型は 「ふつうの概念」として扱われる。 |
| `title` | recommended | `text` | lint と外部ツール | 1枚目の見出しと同じ文字列。表示名の正本は見出しのほうで、ここは写し。 |
| `short` | recommended | `text` | Wiki の表示 | デッキの短い呼び名。**Wiki の各スライドの右上と、そのデッキへ渡るリンクの 末尾に、この文字列がそのまま出る**。両方に出るので短く保つ（全角4字・ 半角6字が目安）。名乗らなければデッキ名（slug）で代替するので、 欠けても壊れないが、そこへ渡るリンクだけ長い語が出て語調が揃わない。 |
| `description` | recommended | `text` | 絞り込み | 1行の説明。**サイドバーの絞り込みに流れる**ので、引きたい言葉を入れる。 |
| `resource` | optional | `uri` | lint と外部ツール | このデッキが説明している実体の URI（OKF の推奨キー）。 考えを書いたデッキには無い — 実在の資産を説明するときだけ名乗る。 |
| `tags` | recommended | `list-of-text` | 絞り込み | デッキの主題。**サイドバーの絞り込みに流れる**ので、題に出ない言葉を補う。 |
| `category` | optional | `text` | lint と外部ツール | デッキの区分。tags が主題を並べるのに対し、こちらは1つだけ選ぶ。 |
| `status` | optional | `draft` / `stable` / `deprecated` | lint と外部ツール | 書きかけかどうか。order.yaml のコメントに書いていた「まだ無い」を機械に見せる。 |
| `author` | optional | `text` | lint と外部ツール | 書いた人。複数なら読点で連ねる（機械に配るなら sources を使う）。 |
| `created` | optional | `date` | lint と外部ツール | 最初に書いた日。git が持っているが、デッキを移しても残る形で持たせる。 |
| `updated` | optional | `date` | lint と外部ツール | 最後に手を入れた日。**手で書く以上ずれる**ので、ずれても困らない用途にだけ使う。 |
| `sources` | optional | `list-of-objects` | lint と外部ツール | このデッキが依拠した資料。地の文に埋まっていた出典を機械可読にする。 |
| `verified` | optional | `list-of-objects` | lint と外部ツール | 誰がいつ内容を確かめたか。出典の照合結果を残す場所。 |
| `generated` | optional | `object` | lint と外部ツール | 機械が作ったデッキであることの表明。 |
| `stale_after` | optional | `date` | lint と外部ツール | この日を過ぎたら見直す。**落ちたら直すのは日付ではなく中身**。 |
| `theme` | optional | `uri` | **まだ効かない** | 使いたいテーマ YAML のパス。**まだ効かない** — テーマは1サイトに1つで、 デッキごとに違うものを選ぶと同じ座標系で描けなくなる。効かせるなら 「サイト全体で1つだけ宣言してよい」規則が先に要る。今は --theme が正本。 |
| `site_title` | optional | `text` | **まだ効かない** | サイトのタイトル。**まだ効かない** — サイトはデッキの集合なので、 どの1デッキが全体の名前を名乗るかを決める規則が先に要る。今は --site-title が正本。 |

1行目がちょうど `---` で、2行目が `key: value` の形のときだけメタとして読む（どちらかを満たさない `---` は今までどおりスライド区切り）。

**書くのはデッキ1本の事実であって、スライド1枚の事実ではない。**
スライドのメタは注釈ディレクティブ（`<!--id:-->` `<!--icon:-->` `<!--takeaway-->`）が担う。
1ファイルに何十枚も入っているので、frontmatter にスライドの話を書くと必ず嘘になる
——「このデッキのタグ」であって「このスライドのタグ」ではないため。

`title` は本文からの派生の写しで、**表示名の正本は今までどおり1枚目の見出し**。
バンドルを読む側（OKF の消費者・GitHub の表）に読ませるために置くだけなので、
食い違ったら直すのは frontmatter のほう。

**キーの名前は OKF v0.2 に合わせてある**（`type` `title` `description` `tags`
`resource` `status` `sources` `verified` `generated` `stale_after`）。
`short` `category` `author` `created` `updated` `theme` `site_title` は OKF に無い
こちらの拡張で、OKF は「知らないキーは保存して拒まない」と定めているので
バンドルとしての適合は保たれる。読む側に意味が届くのは前者だけ、と思って書く。

人が md を開いたとき本文が下がるので、短く保つ（description は1行、tags は5個まで）。

**デッキの並び順はここに置かない。** 正本はディレクトリ直下の `order.yaml` で、
1本の並び列だからこそ「第1部と第2部は対なので隣り合わせに置く」という意図が
書けている。デッキごとの番号に散らすとその意図が消え、間に挿入するたびの
振り直しと、2つの宣言が食い違ったときの規則が要るようになる。

名乗っていない md の扱いは `ignore`、宣言に無いキーは `ignore`、読めない frontmatter は `error`。

## 注釈ディレクティブ

| 注釈 | 記法 | 効くレイアウト | 説明 |
|---|---|---|---|
| `id` | `<!--id:<slug>-->` | すべて | スライドの ID。`デッキ名.md#…` の解決先であり HTML の `#hash` でもある。 |
| `icon` | `<!--icon:mi:<name>-->` | Steps・IconColumns・IconCards | セクションのアイコン。`mi:` 接頭辞で Material Icons、それ以外は絵文字。 |
| `takeaway` | `<!--takeaway-->` | Steps・NumberedList・Table・TextOnly・Default・IconColumns・IconCards・Grid・LeftRight・TopBottom | スライド末尾のまとめ。マーカーの次の行以降が本文になる（典拠は source）。 |
| `source` | `<!--source-->` | WikiPattern | そのスライドの主張の典拠。マーカーの次の行以降が本文になる。 |

- `id` — 省略するとスライドタイトルの slug を採り、衝突したら連番（`-2`）を付ける。 採番は parser/slide-ids.ts が一括で行う。 **明示した ID は予約である。** 他のスライドの見出しから作られた自動 slug より強く、 同じ綴りを欲しがった見出しのほうが連番に回る。したがってリンクの行き先は md の並び順で変わらない。 **明示 ID 同士がぶつかった場合は一意化を続ける**（重複するとリンクが解決できない）。 どちらが間違っているかは機械には決められないので、lint の `slide-id` が 両方の行番号を報告する（`--strict` で終了コード 1）。折れたリンクと違い リンクは解決してしまう — 常に最初の1枚へ — ので、報告が無いと気づけない。

- `icon` — 効くのは annotations に icon を挙げたレイアウトだけ。他では黙って捨てられる。

- `takeaway` — 効くのは annotations に takeaway を挙げたレイアウトだけ。

- `source` — **`takeaway` とは役割が違う。** takeaway は読み手に次を示す「まとめ」で、 本文と同じ大きさで中央に置く。`source` は主張がどこから来たかの**典拠**で、 本文の1/3以下の大きさで下端に伏せる（読まずに飛ばせることが要件）。 役割が違うので**同居しない** — `source` を受けるのは WikiPattern だけで、 その WikiPattern は takeaway を受けない。 **リンクを書いてもリンクにならない。** 出典は参照ではないので、 バックリンクのグラフに載せない（載せると「関連」と「典拠」が混ざり、 文献名がパターンの隣人として並ぶ）。 効くのは annotations に source を挙げたレイアウトだけ。 挙げていないレイアウトに書くと本文ごと消えるので、lint が `annotation-scope` で報告する（`--strict` で終了コード 1）。

## レイアウト一覧

| レイアウト | ディレクティブ | 説明 |
|---|---|---|
| WikiPattern | `<!--pattern-->` | Wiki のパターン1件。左に いつ・なにが困るか／そこで、右に SVG の図解 |
| LeanCanvas | `<!--lean-canvas-->` | リーンキャンバス |
| CustomerJourney | `<!--カスタマージャーニー:-->` | カスタマージャーニーマップ |
| Steps | `<!--steps-->` | 階段状のステップ表示 |
| NumberedList | `<!--numbered-list:circle-->` | 番号付きリスト（意匠は `circle` か `bar`） |
| Agenda | `<!--agenda-->` | TOC/アジェンダ |
| Quote | `<!--quote-->` | 引用・名言スライド |
| Table | `<!--table-->` | テーブル表示（ディレクティブの後にパイプ区切りの表を置く） |
| CodeDisplay | `` ```<language> `` | シンタックスハイライト付きコード |
| TextOnly | `<!--text-only-->` | 自由形式テキスト |
| Default | (なし) | セクションを縦に並べる |
| IconColumns | `<!--icon-cols-->` | アイコン付き3カラム |
| IconCards | `<!--icon-cards-->` | アイコン付きカード |
| Grid | `<!--grid:RxC-->` | R行×C列のグリッド |
| LeftRight | `<!--left:N-->` `<!--right:M-->` | 左右分割（比率指定） |
| TopBottom | `<!--top:N-->` `<!--bottom:M-->` | 上下分割（比率指定） |

## レイアウトごとの構造

`###` / `####` に何を書けばよいかの正本。

### WikiPattern

- ディレクティブ: `<!--pattern-->`
- `_tag`: `WikiPattern`（プラグイン `wiki-pattern`）
- 説明: Wiki のパターン1件。左に いつ・なにが困るか／そこで、右に SVG の図解
- 効く注釈: `id`・`source`

**見出しは2つ。** 状況と問題は地続きの1つの話なので `いつ・なにが困るか` に束ね、
打ち手は `そこで` で受ける（「こういう場面で、こう困る。**そこで**、こうする」）。
節を分けずに束ねるのは、状況と問題を別々の話として読ませないため。

**節の中の空行は段落の切れ目**として残り、2段落目からは見出しの無い節として
同じ間隔で積まれる（節をまたぐ隙間と同じ 0.15in）。だから
`いつ・なにが困るか` は「場面の段落」＋「困りごとの段落」の2つに割り、
**困りごとの核は太字で1文に立てる**。

**分量は 2行 + 3行 + 3行**（場面・困りごと・打ち手）、**1行は全角26字まで**。
左段に入る本文は 2.84in ＝ 9行ぶんしかなく、超えた枚だけ箱が縮んで
文字が箱からはみ出す（`validateLayout` は縮めた後を見るので止まらない）。
26字を超えるとブラウザが折り返して、見積り（1em で数える）と1行ずれる。
見出しが1つ減って増えたのは1行ぶんなので、増やすのは説明であって列挙ではない。

**左段の幅のほうが、この26字から決まっている**（`WP_LEFT_RATIO`）。本文の箱は
5.33in で、全角1字の送りを 1.05em と見た 26字ぶん。左段の使い残しは本文と図の
間の余白としてそのまま見えるので、余らせずに図へ渡してある。だから
**1字は図の幅 0.2in と同じもの**で、字数を増やすことと図を細くすることは
同じ操作になる（25字なら図は 3.80in、26字なら 3.59in、27字なら 3.36in）。

**関連は文中に溶かす。** 隣のパターンへのリンクは、それが要る場所（多くは `そこで`）に
置く。末尾に `関連:` の行を積むと、リンクを踏む理由が本文から切り離され、
パターンの数だけ同じ形の行が並ぶ。だから**パターンは `takeaway` を受けない**
（書いても黙って捨てられ、lint が `annotation-scope` で報告する）。
**主張の典拠は関連ではない** ので `<!--source-->` が別に引き受ける。

**図解は必須。** SVG は md に書き込まず、**別ファイルに置いて `![…](….svg)` で参照する**
（書き忘れたスライドは lint が報告し、変換でも止まる）。パターン・ランゲージは構造を
一目で掴ませる形式なので、絵の無いパターンを置けないようにしてある。
ファイルにするのは、md をそのまま GitHub で開いたときに図が絵として表示されるため。
SVG 側は `viewBox` と、それに合った `width` / `height` を**実寸で**名乗ること
（md での表示サイズになる）。埋め込むときは 100% に読み替え、右の下敷きのほうを
`viewBox` の縦横比に合わせて組み、上を左段の最初の見出しにそろえて置く。だから図は下敷きいっぱいに広がり、
余白（レターボックス）も歪みも出ない。`viewBox` を名乗らない図だけは形が分からず、
従来どおり列いっぱいの下敷きに入る。

**枠は中身に寄せる。** 図が下敷きいっぱいに広がるということは、`viewBox` に残した
余白がそのまま**絵の小ささ**になるということでもある（340x320 の枠に 283x248 しか
描いていない図は、同じ場所に 12% 小さく出る）。描き終えたら
`npx tsx src/tools/trim-svg.ts` を通す — 中身から枠を決めるので何度通しても同じで、
はみ出して切れている図は逆に枠を広げて収める。

| スロット | 記号 | 個数 | 見出し | 本文 | 説明 |
|---|---|---|---|---|---|
| `sections` | `###` | `2` | 語彙 `wiki-pattern-sections` | `free` | いつ・なにが困るか／そこで の2枠。左カラムに上から積まれる。 本文中の空行は段落の切れ目として残り、2段落目からは見出しの無い節になる。 |
| `diagram` | `![…](….svg)` | `1` | 自由 | `none` | 右カラムに置く SVG ファイルへの参照。1スライドにつき必ず1つ。 |

```markdown
## 3ストライクで書く
<!--pattern-->
### いつ・なにが困るか
一度うまくいったやり方を、すぐ書きたくなる。
実感があるうちに書かないと、忘れそうに思える。

**一度きりの成功は、偶然と区別がつかない。**
パターンは発明ではなく発見である。
早く書いたぶん、外れたときに剥がす手間が増える。

### そこで
**別々の場面で3度目に出会うまで書かない。**
3度で [ザワつく状況](patterns-meta.md#ザワつく状況) が見え、構造が残る。
それまでは [種ノート](patterns-wiki.md#種ノート) に置く。

![3ストライクで書く](diagrams/patterns-meta/3ストライクで書く.svg)

<!--source-->
名前は『リファクタリング』の "Three strikes and you refactor"（Don Roberts 由来）。内容は Coplien の rule of three (1996)＝文書化されたパターンは有意に異なる既知の適用例を3件示さねばならず、パターンは発明も開発もされず観察される。
```

### LeanCanvas

- ディレクティブ: `<!--lean-canvas-->`
- `_tag`: `LeanCanvas`（プラグイン `lean-canvas`）
- 説明: リーンキャンバス
- 効く注釈: `id`

**見出し名が配置を決める**（書いた順ではない）。語彙外の見出しはどのマスにも
入らず黙って消えるので、下記 lean-canvas-blocks の語彙から選ぶ。

| スロット | 記号 | 個数 | 見出し | 本文 | 説明 |
|---|---|---|---|---|---|
| `blocks` | `###` | `1..9` | 語彙 `lean-canvas-blocks` | `free` | 9ブロックのいずれか。同じブロックを2回書くと後勝ちになる。 |

```markdown
## リーンキャンバス
<!--lean-canvas-->
### 課題
- 開発コスト高
### ソリューション
- テンプレート提供
### 独自の価値提案
最速でMVP構築できる統合プラットフォーム
```

### CustomerJourney

- ディレクティブ: `<!--カスタマージャーニー:-->`
- `_tag`: `CustomerJourney`（プラグイン `customer-journey`）
- 説明: カスタマージャーニーマップ
- 効く注釈: `id`

フェーズが5件以上あると4件ずつのスライドに自動分割され、タイトルに `(1/2)` が付く。

| スロット | 記号 | 個数 | 見出し | 本文 | 説明 |
|---|---|---|---|---|---|
| `phases` | `###` | `1..n` | 自由 | `none` | フェーズ（列）。名前は自由。 |
| `rows` | `####` | `1..4` | 語彙 `journey-rows` | `bullets-only` | 行ラベル。**`- ` で始まる行しか読まれない**（素の本文行は黙って捨てられる）。 |

```markdown
## カスタマージャーニー
<!--カスタマージャーニー:-->
### 認知
#### タッチ:
- Web検索
#### 行動:
- 情報収集
#### 判断:
- 課題解決できそう
#### 感情:
- 期待感
```

### Steps

- ディレクティブ: `<!--steps-->`
- `_tag`: `Steps`（プラグイン `steps`）
- 説明: 階段状のステップ表示
- 効く注釈: `id`・`icon`・`takeaway`

3件未満だとこのレイアウトとして認識されず Default に落ちる。

| スロット | 記号 | 個数 | 見出し | 本文 | 説明 |
|---|---|---|---|---|---|
| `steps` | `###` | `3..n` | 自由 | `lines` | ステップ。見出し・`<!--icon:-->`・本文の順に書く。本文は**1行目が役割名**、 2行目以降が説明になる（行の位置に意味がある唯一のレイアウト）。 |

```markdown
## タイトル
<!--steps-->
### 見る
<!--icon:mi:visibility-->
観察者
全体像を理解し 各フェーズの目的を 説明できる
### 習う
<!--icon:mi:school-->
見習い
先輩を手本に スクリプトを組む
### 実践
<!--icon:mi:directions_run-->
実践者
自律的に判断し 適切な手法を 選択できる
```

### NumberedList

- ディレクティブ: `<!--numbered-list:circle-->`
- `_tag`: `NumberedList`（プラグイン `numbered-list`）
- 説明: 番号付きリスト（意匠は `circle` か `bar`）
- 効く注釈: `id`・`takeaway`

`<!--numbered-list:circle-->`（丸）か `<!--numbered-list:bar-->`（帯）のどちらかを書く。

| スロット | 記号 | 個数 | 見出し | 本文 | 説明 |
|---|---|---|---|---|---|
| `items` | `###` | `1..n` | 自由 | `free` | 項目。番号は自動採番される（見出しに番号を書かない）。 |

```markdown
## タイトル
<!--numbered-list:circle-->
### 仮説を立てる
観察事実から「〜ならば〜だろう」の形で書く。
### 検証計画を作る
仮説ごとに反証シグナルを設計する。
<!--takeaway-->
仮説検証は1週間スプリントで回すこと
```

### Agenda

- ディレクティブ: `<!--agenda-->`
- `_tag`: `Agenda`（プラグイン `agenda`）
- 説明: TOC/アジェンダ
- 効く注釈: `id`
- ディレクティブ直後の本文: サブタイトル（省略可）。

項目はリンクにでき、Wiki の目次として機能する。

| スロット | 記号 | 個数 | 見出し | 本文 | 説明 |
|---|---|---|---|---|---|
| `items` | `###` | `1..n` | 自由 | `none` | アジェンダ項目。本文は持たない。 |

```markdown
## Agenda
<!--agenda-->
本日のプログラム
### イントロダクション
### 市場分析
### Q&A
```

### Quote

- ディレクティブ: `<!--quote-->`
- `_tag`: `Quote`（プラグイン `quote`）
- 説明: 引用・名言スライド
- 効く注釈: `id`
- ディレクティブ直後の本文: 引用文そのもの。複数行・複数段落も可。

| スロット | 記号 | 個数 | 見出し | 本文 | 説明 |
|---|---|---|---|---|---|
| `author` | `###` | `0..1` | 自由 | `none` | 著者名。省略可（引用文だけのスライドになる）。 |

```markdown
## 名言 — イノベーション
<!--quote-->
イノベーションとは、優れたアイデアに「イエス」と言うことではない。
### Steve Jobs
```

### Table

- ディレクティブ: `<!--table-->`
- `_tag`: `Table`（プラグイン `table`）
- 説明: テーブル表示（ディレクティブの後にパイプ区切りの表を置く）
- 効く注釈: `id`・`takeaway`
- ディレクティブ直後の本文: パイプ区切りのテーブル本体。

ディレクティブの後にパイプ区切りの表を置く。`| --- |` の区切り行が必要。
セル内のテキストはプラグインが直接描くのでリンクは効かない（BACKLOG B-22）。

```markdown
## 機能比較
<!--table-->
| 機能 | Basic | Pro |
| --- | --- | --- |
| ユーザー数 | 5名 | 50名 |
| サポート | メール | 優先対応 |
```

### CodeDisplay

- ディレクティブ: `` ```<language> ``
- `_tag`: `CodeDisplay`（コアレイアウト）
- 説明: シンタックスハイライト付きコード
- 効く注釈: `id`
- ディレクティブ直後の本文: なし（コードフェンスの後の本文がキャプションになる）

文字数制限の対象外（タイトルだけ数える）。コードフェンスの**後ろ**に書いた本文は
キャプションになる。

```markdown
## TypeScript コード例

```typescript
interface User {
  id: number
  name: string
}
```

型安全なユーザー型。
```

### TextOnly

- ディレクティブ: `<!--text-only-->`
- `_tag`: `TextOnly`（プラグイン `text-only`）
- 説明: 自由形式テキスト
- 効く注釈: `id`・`takeaway`
- ディレクティブ直後の本文: 本文そのもの。空行で段落が分かれる。

```markdown
## エグゼクティブサマリー
<!--text-only-->
本レポートは、開発者が直面する構造的な問題を分析したものです。

分析の結果、単なる時間管理の問題ではないことが明らかになりました。
```

### Default

- ディレクティブ: (なし)
- `_tag`: `Default`（コアレイアウト）
- 説明: セクションを縦に並べる
- 効く注釈: `id`・`takeaway`

| スロット | 記号 | 個数 | 見出し | 本文 | 説明 |
|---|---|---|---|---|---|
| `sections` | `###` | `0..n` | 自由 | `free` | 見出しと本文の組。上から順に縦に積まれる。`###` を書かず本文だけを置くと、 見出しの無いセクション1件として扱われる（だから 0 件でも成立する）。 |

```markdown
## スライドタイトル
### 見出しA
本文テキスト
### 見出しB
本文テキスト
```

### IconColumns

- ディレクティブ: `<!--icon-cols-->`
- `_tag`: `IconColumn`（プラグイン `icon-cols`）
- 説明: アイコン付き3カラム
- 効く注釈: `id`・`icon`・`takeaway`

**3件ちょうど**を想定する。2件以下だとこのレイアウトとして認識されず Default に落ち、
4件以上だと4件目以降が黙って捨てられる（BACKLOG B-07 で可変化を検討中）。

| スロット | 記号 | 個数 | 見出し | 本文 | 説明 |
|---|---|---|---|---|---|
| `columns` | `###` | `3` | 自由 | `free` | カラム。見出し・`<!--icon:-->`・本文の順に書く。 |

```markdown
## タイトル
<!--icon-cols-->
### 1. 自己認識
<!--icon:mi:search-->
無意識的な前提を特定する力。
### 2. 率直さ
<!--icon:💬-->
前提の崩壊を認める誠実さ。
### 3. 責任
<!--icon:⚓-->
不確実性を引き受ける覚悟。
```

### IconCards

- ディレクティブ: `<!--icon-cards-->`
- `_tag`: `IconCard`（プラグイン `icon-cards`）
- 説明: アイコン付きカード
- 効く注釈: `id`・`icon`・`takeaway`

IconColumns と同じく **3件ちょうど**を想定する（過不足の扱いも同じ）。

| スロット | 記号 | 個数 | 見出し | 本文 | 説明 |
|---|---|---|---|---|---|
| `columns` | `###` | `3` | 自由 | `free` | カード。見出し・`<!--icon:-->`・本文の順に書く。 |

```markdown
## タイトル
<!--icon-cards-->
### 認知科学
<!--icon:mi:psychology-->
人間の思考プロセスを理解する研究分野。
### コーディング
<!--icon:mi:code-->
効率的なアルゴリズムで構築する。
### 自動化
<!--icon:mi:auto_awesome-->
反復タスクを自動化する。
```

### Grid

- ディレクティブ: `<!--grid:RxC-->`
- `_tag`: `Grid`（コアレイアウト）
- 説明: R行×C列のグリッド
- 効く注釈: `id`・`takeaway`

フォントサイズは R×C から自動で決まる（1x1 が最大、4x4 が最小）。

| スロット | 記号 | 個数 | 見出し | 本文 | 説明 |
|---|---|---|---|---|---|
| `cells` | `###` | `rows*cols` | 自由 | `free` | セル。左上から行優先で埋まる。件数が R×C と違うと空セルか溢れになる。 |

```markdown
## タイトル
<!--grid:2x2-->
### セル1
内容1
### セル2
内容2
### セル3
内容3
### セル4
内容4
```

### LeftRight

- ディレクティブ: `<!--left:N-->` `<!--right:M-->`
- `_tag`: `LeftRight`（コアレイアウト）
- 説明: 左右分割（比率指定）
- 効く注釈: `id`・`takeaway`

N・M は整数の比率。`right` は `left` より後に置く（前に置くと ParseError）。

| スロット | 記号 | 個数 | 見出し | 本文 | 説明 |
|---|---|---|---|---|---|
| `sections` | `###` | `0..n` | 自由 | `free` | 直前のディレクティブが指す側（左/右）に積まれる。本文だけでも成立する。 |

```markdown
## タイトル
<!--left:2-->
### 左側
左の内容
<!--right:1-->
### 右側
右の内容
```

### TopBottom

- ディレクティブ: `<!--top:N-->` `<!--bottom:M-->`
- `_tag`: `TopBottom`（コアレイアウト）
- 説明: 上下分割（比率指定）
- 効く注釈: `id`・`takeaway`

N・M は整数の比率。`bottom` は `top` より後に置く（前に置くと ParseError）。

| スロット | 記号 | 個数 | 見出し | 本文 | 説明 |
|---|---|---|---|---|---|
| `sections` | `###` | `0..n` | 自由 | `free` | 直前のディレクティブが指す側（上/下）に積まれる。本文だけでも成立する。 |

```markdown
## タイトル
<!--top:3-->
### 上側
上の内容
<!--bottom:2-->
### 下側
下の内容
```

## 語彙

見出し名そのものが意味を持つスロットで、受理される名前の一覧。

### Wiki パターンの2節（`wiki-pattern-sections`）

語彙外の見出しの扱い: **error** — どの枠にも入らず、その節は描かれない

2節そろって1つのパターンになる。順番は書いた順ではなく、この語彙の並び
（いつ・なにが困るか → そこで）で左カラムに積まれる。

**語彙外の見出しはエラーにする。** 3節だった頃の `### 問題` を書き残すと、
警告のままでは中身ごと黙って消えた状態で公開されてしまう。ここだけ `error` なのは、
1パターンの節が2つしかなく、1つ落ちれば残りが片肺だと分かりきっているため。

| キー | 正書 | 別表記 | 説明 |
|---|---|---|---|
| `situation` | いつ・なにが困るか | `状況`・`いつ`・`situation` | どんな場面で、何が困るのか。本文がそのまま入る。 空行で「場面の段落」と「困りごとの段落」に割り、困りごとの核は太字にする。 |
| `solution` | そこで | `解決`・`どうする`・`solution` | 打ち手。本文がそのまま入る。冒頭の一文を太字にして打ち手を立てる。 |

### リーンキャンバスの9ブロック（`lean-canvas-blocks`）

語彙外の見出しの扱い: **warning** — どのマスにも入らず、そのブロックは描かれない

| キー | 正書 | 別表記 | 説明 |
|---|---|---|---|
| `problem` | 課題 | `problem` | 上位3つの課題 |
| `solution` | ソリューション | `solution`・`解決策` | 課題に対する打ち手 |
| `uvp` | 独自の価値提案 | `unique value proposition`・`uvp`・`価値提案` | なぜ違うのか・なぜ注目に値するのか |
| `advantage` | 圧倒的な優位性 | `unfair advantage`・`advantage`・`優位性` | 簡単に真似できない強み |
| `customer` | 顧客セグメント | `customer segments`・`customer`・`顧客` | 対象とする顧客 |
| `metrics` | 主要指標 | `key metrics`・`metrics`・`指標` | 事業の健康を測る数値 |
| `channels` | チャネル | `channels` | 顧客への経路 |
| `cost` | コスト構造 | `cost structure`・`cost`・`コスト` | 固定費・変動費 |
| `revenue` | 収益の流れ | `revenue streams`・`revenue`・`収益` | 収益モデル |

### カスタマージャーニーの行ラベル（`journey-rows`）

語彙外の見出しの扱い: **warning** — その `####` 以下の箇条書きはどの行にも入らず描かれない

末尾のコロン（`:` / `：`）は書いても書かなくてよい（除去して照合する）。

| キー | 正書 | 別表記 | 説明 |
|---|---|---|---|
| `touch` | タッチ | — | 顧客との接点 |
| `action` | 行動 | — | 顧客の行動 |
| `judge` | 判断 | — | 顧客の判断・評価 |
| `emotion` | 感情 | — | 顧客の感情 |

### frontmatter の type（`deck-types`）

語彙外の見出しの扱い: **warning** — 外部ツールが種別で絞れなくなる（描画には影響しない）

スロットの見出しではなく **frontmatter の `type` から参照される**唯一の語彙。
値が増えるのは「md ファイルの種類」が増えたときだけなので、
レイアウトや注釈のように書き手が日々選ぶものではない。

| キー | 正書 | 別表記 | 説明 |
|---|---|---|---|
| `deck` | deck | — | スライドのデッキ（doc/wiki/*.md） |
| `spec` | spec | — | 記法の仕様書（doc/Spec.md） |

## バンドル（サイトの階層）

デッキを収めたディレクトリ1つ。そのまま配れば他のエージェントが読める Open Knowledge Format のバンドルになる。版は `0.2`（[SPEC.md](https://github.com/GoogleCloudPlatform/knowledge-catalog/blob/main/okf/SPEC.md)）。

**予約ファイル名**

| ファイル名 | 役割 | 説明 |
|---|---|---|
| `index.md` | 目録 | バンドルに何が入っているかの一覧（SPEC.md §8）。`order.yaml` の `groups:` から生成する。 |
| `log.md` | 更新履歴 | いつ何が変わったかの記録（SPEC.md §9）。デッキが日付を1つも名乗っていなければ作らない。 |

**予約ファイルはデッキではない。** 概念を書いたものではなくバンドル自身についての
メタなので、スライドとして読み込まず、内部リンクの行き先にもしない
（指すと外部リンクとして描かれる）。`order.yaml` に書くこともできない。

**デッキの集合と並び**

- デッキ集合: ディレクトリ直下の `*.md` から予約ファイル名を除いたもの
- 並び順の正本: `order.yaml`（`groups: [{title, decks}]`）

デッキ名は**拡張子を除いたファイル名**で書く。グループ名は生成される目録の見出しに
なるので、「第1部と第2部は対」といった意図が機械にも読める場所に載る。
宣言が無ければファイル名順。

宣言に無いデッキは末尾へ回す（追記を忘れたデッキがサイトから黙って消えないため）。
宣言にあって存在しないデッキ名はビルドを止める。

**デッキ slug**

- もと: 拡張子を除いたファイル名
- 綴りの規則: 見出し → スライド ID と同じ1つの規則（`src/slug.ts`）
- 衝突したときの扱い: `error`

| ファイル名 | デッキ slug | リンクの書き方 |
|---|---|---|
| `patterns-wiki.md` | `patterns-wiki` | `patterns-wiki.md#スライドID` |
| `My_Deck.md` | `my-deck` | `My_Deck.md#スライドID` |
| `Wiki の作り方.md` | `wiki-の作り方` | `Wiki の作り方.md#スライドID` |
| `種ノート.md` | `種ノート` | `種ノート.md#スライドID` |

**`order.yaml` に書く名前・リンクに書くファイル名・サイトの slug は同じものを指す。**
3つが同じ規則を通るからそう言える。ファイル名は `デッキ名.md#スライドID` の
行き先そのものなので、並び替えのためにリネームするとサイト中のリンクが折れる。

slug が衝突する2つの md は誤りとして止める。デッキはファイルなので書き手が改名できる
（スライド ID と違い、機械が一意化して先へ進む理由が無い）。

**スライド ID の一意性**

- 一意な範囲: デッキ
- サイト全体での綴り: `{deck-slug}/{slide-id}`

ID がデッキ内で閉じているのは、単体ファイルを `--html` にかけたときに短く安定した
ID であってほしいため。サイトにまとめる段で `deck-slug/` を前置して名前空間化する。

**閉じているからこそ、ID の衝突の検査を lint に置けた** — lint は1ファイルずつ
呼ばれるので、検査の範囲と一意性の範囲がそのまま一致する。

**バンドルは平坦である。** 読むのはディレクトリ直下の md だけで、サブディレクトリは
降りない。内部リンクがパス区切りを含む形を受けないのはそのためで、`sub/deck.md` は
内部リンクにならない。ここで basename だけを採ると、`deck.md` と `sub/deck.md` が
同じ slug に落ちて「同名の最上位デッキに黙って当たる」形の誤りが入り込む。

**平坦だから、相対リンクの解決に元ドキュメントの位置が要らない。** デッキは全部が
兄弟なので、`x.md` はどのデッキから書かれても同じ1枚を指す。階層を許すとこの性質が
消え、リンクを読む側が「どのファイルに書かれたリンクか」を持ち歩くことになる
（→ `inline` 節の `internal-link`）。

**この節が宣言するのは綴りと規則だけで、正規化の中身は持たない。**
デッキ slug の作り方は `deck-slug` が「見出し → ID と同じ1つの規則を通る」と
言うにとどめ、規則そのものは実装（`src/slug.ts`）に1本だけ置く。
**両側が同じ規則で作られること自体が保証なので、規則を2箇所に書いた時点でその保証が消える。**

**CLI の打ち方はここに書かない。** 宣言はコマンドを持たない。目録と履歴の作り方は
SKILL.md が、実装の入口は CLAUDE.md が持つ。

## インライン記法

| 書き方 | 意味 |
|---|---|
| `**text**` | 太字 |
| `*text*` | 斜体 |
| `` `text` `` | インラインコード |
| `[ラベル](https://example.com)` | 外部リンク。HTML は `<a>`、PPTX はハイパーリンク。 |
| `[ラベル](デッキ名.md#スライドID)` | 内部リンク。**デッキ名だけの相対パス**で書く。同じデッキの中でもデッキ名を書く。 フラグメントを省くとそのデッキの1枚目に着く。解決できなければ未解決として報告する。 |

効くのは `section-heading`・`section-body`・`takeaway`。プラグインがテキストを直接描く箇所（テーブルのセル・引用本文・ステップのラベル等）では リンクはそのままの文字として出る（BACKLOG B-22）。

**internal-link**

**先頭に `/` も `./` も付けない。** 書く形は `デッキ名.md#スライドID` だけである。

理由は GitHub で当たること。先頭に `/` を付けると、github.com はそれを
**バンドルのルートではなくリポジトリのルート**と読む。バンドルはリポジトリの
深い場所にあるので、生の md を開いた読者にはリンクが折れて見える。
相対形なら置き場所の深さに関係なく当たるので、綴りが1つで両方に効く。

OKF v0.2 §6.1 は相対も絶対も許し、絶対形を「推奨」と書く。ただしその理由は
「概念をサブディレクトリの中で移してもリンクが折れない」ことなので、
サブディレクトリを持たないこのバンドルには効かない（→ `okf` 節）。

**読むほうは `./x.md` と `/x.md` も解決する。** 書く形を絞るのは lint の仕事で、
パーサに蹴らせてはいけない。蹴られたリンクは外部リンクとして `target="_blank"`
で描かれ、未解決リンクの一覧にも出ない — **見た目はリンクのまま黙って壊れる。**
読みを広く、書きを狭く。

**次の2つは内部リンクにしない。**

- `sub/x.md` `../x.md` — バンドルが平坦なので、指す先が存在しない
- デッキ名を省いた `#id` — どのデッキに書かれたリンクかを知らないと解決できず、
  「href だけ見れば行き先が決まる」という性質が壊れる（その性質の根拠 → `okf` 節）

`#スライドID` は OKF の規定外で、この道具の拡張。OKF は1ファイル=1概念だが、
こちらは1ファイルに何十枚も入るので、ファイルだけでは行き先を指せない。

予約ファイル名（→ `okf` 節の `reserved-files`）はデッキではないので、
指しても内部リンクにはならず外部リンクとして描かれる。

## 制限

- 1スライド **1000文字**を超えると ValidationError。本文と見出し（Markdown 構文を除く）。リンクは表示ラベルだけを数え、URL は数えない。
- 読みやすさの目安は **240文字程度**（ツールでは強制しない）
- `CodeDisplay` は文字数を数えない（タイトルのみ）

上限はレイアウトごとに `max-chars` で上書きできる。超過は ValidationError で止まる。
上限の中でも読みやすさの目安は 240 文字程度で、これはツールでは強制しない。
