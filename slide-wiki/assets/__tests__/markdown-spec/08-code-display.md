# Code Display Feature Test

---

## TypeScript Example

```typescript
function greet(name: string): string {
  return `Hello, ${name}!`
}
```

This is a simple greeting function with type annotations.

---

## Python Example

```python
def factorial(n):
    if n == 0:
        return 1
    return n * factorial(n - 1)
```

Recursive factorial implementation

---

## JavaScript Without Caption

```javascript
const sum = (a, b) => a + b
console.log(sum(5, 3))
```

---

## Plain Text Code

```plaintext
This is plain text code block
No syntax highlighting applied
Just monospace font
```
